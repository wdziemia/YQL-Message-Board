<table xmlns="http://query.yahooapis.com/v1/schema/table.xsd">
  <meta>
    <author>Wojciech Dziemianczyk</author>
    <description>Yahoo Message Board</description>
    <sampleQuery>SELECT * FROM {table} WHERE symbol='GOOG';</sampleQuery>
  </meta>

<bindings>
  <select itemPath="" produces="XML">
  <urls>
  	<url>http://finance.yahoo.com/mb/{symbol}/</url>
  </urls>
  <inputs>
  	<key id='symbol' type='xs:string' paramType='path' required="true" />
  </inputs>
  <execute>
    <![CDATA[

	    function getMessages() {
		   
		    return true;
	    }
	
	 	var yQuery = y.rest( {request.url} );	
	    var data = yQuery.accept( "text/html" ).get().response;
		var optionsPath = y.xpath(data, "//table[@id='mb-ajxp-list mb-sorted-by-la']/tr");
		
		getMessages();
		
	    response.object = <item> 
                             <url>{request.url}</url> 
                             <symbol>{symbol}</symbol>
                             <data>optionsPath</data>
                           </item>; 
]]></execute>
</select>
</bindings>
</table>
